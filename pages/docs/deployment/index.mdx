import Sidebar from "@components/docs/Sidebar";
import MDXDocs from "@components/docs/MDXDocs";
import NextPage from "@components/docs/NextPage";
import PageTitle from "@components/docs/PageTitle";
import DocsAd from "@components/docs/DocsAd";
import Contributors from "@components/docs/Contributors";
import Collapse from "@components/docs/Collapse";

<PageTitle title="Deployment Guides" />

<Sidebar />
<MDXDocs>
  <DocsAd />
  # Deployment Guides
  <Contributors contributors={["manuanish"]} readTime={1} />
  ---
  ### 🚀 Server Providers
  Currently, Bon5R supports three main deployment platforms.
  - [Vercel](https://vercel.com)
  - [Heroku](https://heroku.com)
  - [Github Pages](https://pages.github.com)
  - <span className="text-slate-500">Your provider here?</span>

#### Contributing

If you want to know other ways to deploy a Bon5R app/want to add your own provider here, follow these steps:

- Create an [issue](https://github.com/manuanish/Bon5R/issues) describing your suggestion.
- Link a pull request to your issue and wait for a code reviewer to pass all checks.

Contributions are always welcome!

  <br/>
  ### Prerequisites
  Before we can deploy our project to the web, we need to have a repository hosted on GitHub that
  your server provider can access.
  <br/>
  <Collapse title="📖 Create a Github Repository">

#### 1. Initialize Git

Run the following command in the root directory of your project:

```shell
git init -b main
```

  <br/>
  #### 2. Commit all changes to `main`
  Run the following command in the root directory of your project:
  ```shell
  git add . && git commit -m "initial commit"
  ```
  <br/>
  #### 3. Create a Github Repository
  Run the following command in the root directory of your project:
  ```shell
  gh repo create
  ```
  Follow the prompted steps and your repository should be published on GitHub
  after it is done.
  <br/>
  #### Alternative Methods
  If you want to use GitHub Desktop, or a more interative GUI, I suggest
  you read the docs on Github on [Adding locally hosted code to GitHub](https://docs.github.com/en/get-started/importing-your-projects-to-github/importing-source-code-to-github/adding-locally-hosted-code-to-github).
  </Collapse>
  <br/>
  ### ✨ Demo
  For a live demo of an application bootstrapped with `create-bon5r-app` take a look at the following links:
   - https://create-bon5r-app.vercel.app/

  <br/>
  <br/>
  <Collapse title="💻 Running a local server">
  To run a server on your local computer, follow these steps.

#### 1. Build the application

You can build the application by running the command

```shell
yarn build
```

  <br/>
  #### 2. Start the server
  You can start the local server by running the following command
  ```shell
  yarn start
  ```
  </Collapse>
  <br />
  <br />
  <br />
  <NextPage
    nextPage="Vercel"
    previousPage="Adding Remark/Rehype Plugins"
    prevHref="/docs/customization/adding-markdown-plugins"
    nextHref="/docs/deployment/vercel"
  />
</MDXDocs>
